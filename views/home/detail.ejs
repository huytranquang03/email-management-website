<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Email Detail</title>
		<style>
			body {
				font-family: "Arial", sans-serif;
				margin: 0;
				padding: 0;
				background-color: #f1f1f1;
			}

			.content {
				margin-left: 240px;
				padding: 20px;
				background-color: #fff;
				border-radius: 8px;
				box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
			}

			.navbar {
				background-color: #fff;
				border-bottom: 1px solid #ddd;
				padding: 15px 20px;
			}

			.navbar-brand {
				font-size: 24px;
				font-weight: bold;
				color: #4285f4;
				text-decoration: none;
			}

			.navbar-nav {
				list-style: none;
				margin: 0;
				padding: 0;
			}

			.navbar-nav li {
				display: inline-block;
				margin-right: 15px;
			}

			.navbar-nav li a {
				text-decoration: none;
				color: #555;
				font-size: 16px;
			}

			h1 {
				font-size: 28px;
				color: #333;
				border-bottom: 2px solid #4285f4;
				padding-bottom: 10px;
				margin-bottom: 20px;
			}

			h2 {
				font-size: 20px;
				color: #4285f4;
				margin: 10px 0;
			}

			p {
				font-size: 16px;
				color: #555;
				line-height: 1.5;
			}

			.attachment {
				margin-top: 15px;
				padding: 10px;
				border: 1px dashed #ddd;
				border-radius: 5px;
				background-color: #f9f9f9;
				color: #333;
				font-weight: bold;
			}
		</style>
	</head>

	<body>
		<%- include ("../_partial/_header.ejs"); %> <%- include
		("../_partial/_sidebar.ejs"); %>
		<div class="content">
			<h1>Email Detail</h1>

			<h2>Subject: <%= email.subject %></h2>
			<p>Sender: <%= email.sender_name %></p>
			<p>Message: <%= email.message %></p>
			<p>Timestamp: <%= email.timestamp %></p>

			<% if (email.attachment) { %>
			<div class="attachment">
				<p>
					<a href="/download/<%= email.attachment_path %>"
						>Download Attachment</a
					>
				</p>
			</div>
			<% } %>
		</div>
	</body>
</html>
